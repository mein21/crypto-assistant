{
  "name": "crypto-assistant-bybit-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Local Express proxy + Cloudflare quick tunnel that lets the Vercel app reach Bybit's private API from a non-blocked IP.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "tunnel": "node launch.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cloudflared": "^0.7.0",
    "express": "^4.21.0"
  }
}
